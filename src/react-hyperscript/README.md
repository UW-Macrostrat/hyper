These files were integrated directly from the `react-hyperscript` module
at milestone `3.2.0`. We incorporated this dependency in order to resolve
peer dependencies correctly for recent versions of React.
